<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Ari - Your Relational Companion</title>
  <style>
    body {
      font-family: sans-serif;
      background: #f7f9fb;
      padding: 2rem;
      max-width: 600px;
      margin: auto;
    }
    .chat-box {
      border: 1px solid #ccc;
      background: white;
      padding: 1rem;
      border-radius: 10px;
      height: 400px;
      overflow-y: auto;
      margin-bottom: 1rem;
    }
    .chat-entry {
      margin-bottom: 1em;
    }
    .ari {
      color: #1d4ed8;
    }
    .user {
      color: #374151;
      font-weight: bold;
    }
    input[type="text"] {
      width: 80%;
      padding: 0.5em;
      border: 1px solid #ccc;
      border-radius: 6px;
    }
    button {
      padding: 0.5em 1em;
      margin-left: 0.5em;
      background-color: #1d4ed8;
      color: white;
      border: none;
      border-radius: 6px;
      cursor: pointer;
    }
  </style>
</head>
<body>

  <h2>Hello, I’m Ari – your relational companion</h2>
  <div class="chat-box" id="chatLog">
    <div class="chat-entry ari"><strong>Ari:</strong> Hi there. How are you feeling as we begin, in terms of your connections or experiences with others?</div>
  </div>

  <input type="text" id="userInput" placeholder="Type your message..." />
  <button onclick="sendMessage()">Send</button>

  <script>
    const chatLog = document.getElementById('chatLog');
    const userInput = document.getElementById('userInput');

    async function sendMessage() {
      const text = userInput.value.trim();
      if (!text) return;

      addMessage('You', text, 'user');
      userInput.value = '';

      const response = await fetch('https://ariprototype.onrender.com/interact', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ text })
      });

      const data = await response.json();
      addMessage('Ari', data.reply, 'ari');
    }

    function addMessage(sender, message, className) {
      const div = document.createElement('div');
      div.className = `chat-entry ${className}`;
      div.innerHTML = `<strong>${sender}:</strong> ${message}`;
      chatLog.appendChild(div);
      chatLog.scrollTop = chatLog.scrollHeight;
    }
  </script>

</body>
</html>
